#ifndef APPLICATION__APPLICATION
#define APPLICATION__APPLICATION

#pragma once

#include "controller_interface.h"
#include "hardware_interface.h"
/*
class Application
{
public:
    Application(
        const interfaces::HardwareInterface& hardware,
        const interfaces::ControllerInterface& controller,
        const int num_iters,
    ):
        hardware(hardware), 
        controller(controller),
        num_iters(num_iters) 
    {};

    const interfaces::HardwareInterface& hardware;
    const interfaces::ControllerInterface& controller;
    const int num_iters;

}; // Application 
*/

#endif // APPLICATION__APPLICATION